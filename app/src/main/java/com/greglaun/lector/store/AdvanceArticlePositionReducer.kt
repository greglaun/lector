package com.greglaun.lector.store

import com.greglaun.lector.ui.speak.*

class AdvanceArticlePositionReducer: Reducer {
    override fun reduce(action: Action, currentState: State): State {
        return
    }
}
